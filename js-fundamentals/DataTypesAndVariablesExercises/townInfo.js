function printTownInfo(town, population, area) {
    let info = `Town ${town} has population of ${population} and area ${area} square km.`;
    console.log(info);
}

printTownInfo('Sofia', 1286383, 492);
